[pytest]
testpaths = tests
# tests/ is on the path so the helpers in conftest.py can be imported by name.
pythonpath = . tests
addopts = -q --strict-markers
markers =
    slow: renders real video with ffmpeg (10-30s each). Skip with -m "not slow".
filterwarnings =
    error::DeprecationWarning:reelfactory.*
