# Install with:  pip install -r requirements.txt
PyYAML>=6.0
edge-tts>=6.1.9      # free Microsoft neural voices, no API key
gTTS>=2.5.0          # optional second TTS option
requests>=2.31.0     # calls the Gemini API for --script ai and --tts gemini
Flask>=3.0           # local web UI: python -m reelfactory serve

# Only needed to run the test suite:  python -m pytest
pytest>=8.0
